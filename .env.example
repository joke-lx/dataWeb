# Copy to `.env` and adjust as needed. Consumed by docker-compose*.yml.
# NEVER commit a real .env (it is gitignored).

# Image tag used by docker-compose.prod.yml (latest | <git-sha>).
TAG=latest

# Backend data root inside the api container. Point at an empty/missing dir to
# force deterministic mock data; mount real omics data to ./data for real reads.
DATAWEB_DATA_ROOT=/data

# Optional relational store (post-MVP ingestion phase). The api reads this at
# startup; when unset the DB layer stays disabled and file/mock reads are used.
# For local dev pointing at the cloud Postgres:
#   DATAWEB_DATABASE_URL=postgresql+psycopg://dataweb:CHANGE_ME@<HOST>:5432/dataweb
# DATAWEB_DATABASE_URL=
